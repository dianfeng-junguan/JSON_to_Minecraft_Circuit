# 星期五, 十二月 12 2025

## Tasks

## Notes

今天发现，仿真因为在内部自己获取了import model，所以还需要外部提供库路径，
不想这样，于是在参数里面添加了import model。

另外发现一个问题。
上次写的代码里面connection描述了calcuint之间的连接关系，不够具体，应该具体到powerpoint(port)。
不然就没办法具体对应端口。

上次simulation漏写了通过连接扩散红石信号的逻辑，写上去了，为此还改了一堆代码以符合rust的要求。
真是好痛苦。
明天急须优化代码结构，现在都是巨型函数。